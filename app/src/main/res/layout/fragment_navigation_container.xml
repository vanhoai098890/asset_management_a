<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="data"
            type="com.example.assetmanagementapp.ui.navigationcontainer.MainNavigationContainerViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/vpContainer"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_0dp"
            android:layout_gravity="top"
            android:layout_marginBottom="@dimen/_40dp_negative"
            android:visibility="visible"
            app:layout_constraintBottom_toTopOf="@id/bottomNavigation"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_rectangle_transparent_radius_6dp"
            android:elevation="@dimen/margin_9dp"
            android:fitsSystemWindows="true"
            app:itemHorizontalTranslationEnabled="false"
            app:itemIconSize="@dimen/_24dp"
            app:itemIconTint="@drawable/selector_dynamic_color_bottom_naviation_bar"
            app:itemRippleColor="@null"
            app:itemTextColor="@drawable/selector_dynamic_color_bottom_naviation_bar"
            app:labelVisibilityMode="unlabeled"
            app:layout_constraintBottom_toBottomOf="parent"
            app:menu="@menu/menu_navigation" />

        <ImageView
            android:id="@+id/ivScan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/ic_scan_round"
            android:elevation="@dimen/_10dp"
            app:layout_constraintBottom_toBottomOf="@id/bottomNavigation"
            app:layout_constraintEnd_toEndOf="@id/bottomNavigation"
            app:layout_constraintStart_toStartOf="@id/bottomNavigation"
            app:layout_constraintTop_toTopOf="@id/bottomNavigation"
            tools:ignore="ContentDescription" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
